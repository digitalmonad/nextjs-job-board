/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      { hostname: "rozu6yrpwyat2dqq.public.blob.vercel-storage.com" },
    ],
  },
};

module.exports = nextConfig;
